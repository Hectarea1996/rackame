#lang racket/base

(require vulkan/unsafe)


#|(define (rkm-cmd-clear-image command-buffer image image-layout r g b a)

    (define vk-command-buffer (rkm-command-buffer-vk-command-buffer command-buffer))
    (define vk-))|#